# EnvironmentPostProcessor still registers through spring.factories in Boot 3;
# only auto-configuration moved to AutoConfiguration.imports.
org.springframework.boot.env.EnvironmentPostProcessor=\
com.medicity.config.DatabaseUrlEnvironmentPostProcessor
